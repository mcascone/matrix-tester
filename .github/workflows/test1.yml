name: test1

on: 
    - push
    - workflow_dispatch

jobs:
    create-matrix:
        runs-on: ubuntu-latest
        steps:
            - run: echo hi

    run-matrix:
        runs-on: ubuntu-latest
        steps:
            - run: echo ho
    inspect:
        runs-on: ubuntu-latest
        steps:
            - run: echo oh
